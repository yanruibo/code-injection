.addClass(
.after(
.ajax(
.append(
.attr(
.before(
.each(
.extend(
.find(
.floor(
.get(
.getTime(
.hide(
.html(
.indexOf(
.innerHTML
.insertBefore(
.name
.outerHTML
.parent(
.prepend(
.preventDefault(
.remove(
.removeClass(
.replace(
.replaceAll(
.replaceWith(
.show(
.split(
.substr(
.toLowerCase(
.trigger(
.value
batterystatus batterycritical batterylow
bluetoothSerial.
connectivity.
cordova.file.
cordova.plugins.barcodeScanner.
document.getElementById(
document.write(
document.writeln(
navigator.contacts.
nfc.
parseInt(
sms.send(
window.location
